{
  "name": "holen",
  "version": "0.0.1",
  "description": "Focused ajax library",
  "main": "lib/index.js",
  "scripts": {
    "build:dist": "scripts/build-dist",
    "build:lib": "scripts/build-lib",
    "build:docs": "scripts/build-docs",
    "build": "scripts/build",
    "version": "scripts/version",
    "test": "parallel test:server test:run",
    "test:run": "scripts/test-run",
    "test:server": "scripts/test-server",
    "prepublish": "npm build"
  },
  "keywords": [
    "ajax"
  ],
  "files": [
    "dist",
    "lib",
    "src",
    "docs",
    "LICENSE",
    "README.md",
    "build.hxml"
  ],
  "author": "statianzo",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.0",
    "es6-promise": "^3.3.1",
    "haxeify": "github:statianzo/haxeify",
    "npm-parallel": "^2.0.1",
    "tap-min": "^1.1.0",
    "tape": "^4.6.0",
    "tape-run": "^2.1.4"
  }
}
